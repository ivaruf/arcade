// Shared by the sign (ES module import) and service worker (importScripts).
// Bump this value for every arcade release.
globalThis.GOPHER_CLOUD_VERSION = 'v2.11.13'; // Play! instead of Walk in, and an installed arcade can skip the welcome card
