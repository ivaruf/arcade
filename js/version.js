// Shared by the sign (ES module import) and service worker (importScripts).
// Bump this value for every arcade release.
globalThis.GOPHER_CLOUD_VERSION = 'v2.11.18'; // The letter is just a letter now — the row of game tags is gone and the paper has the room
