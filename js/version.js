// Shared by the sign (ES module import) and service worker (importScripts).
// Bump this value for every arcade release.
globalThis.GOPHER_CLOUD_VERSION = 'v2.11.15'; // The letter sheet keeps its height on a phone, and the tap that opens a panel no longer presses a button inside it
