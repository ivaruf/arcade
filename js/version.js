// Shared by the sign (ES module import) and service worker (importScripts).
// Bump this value for every arcade release.
globalThis.GOPHER_CLOUD_VERSION = 'v2.11.14'; // A mailbox on the welcome cloud that writes back: letters out, answers in, the thread kept half on the device and half in the repo
